# Django Settings
SECRET_KEY=your-secret-key-here-change-this-in-production

# Database Configuration
# For local development (without Docker):
DB_NAME=evara
DB_USER=nihal
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# Note: When using Docker, these DB variables are automatically set by docker-compose.yml
# You only need to set them if running Django locally without Docker

# Legacy database password variable (for backward compatibility)
DATABASE_PASSWORD=your-database-password

# Email Configuration
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Social Authentication (Google OAuth)
SOCIAL_AUTH_GOOGLE_OAUTH2_KEY=your-google-oauth-client-id
SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET=your-google-oauth-client-secret
